# Problem
The project has zero test coverage and no test fixtures. Agents manually construct test trees in their heads when reasoning about store behavior.

# Improvement Needed
Create a reusable test fixture generator that builds realistic tree data structures (small/medium/large) for use in Vitest tests.

# Expected Result
Agents can write store and utility tests quickly without manually constructing tree data objects.
