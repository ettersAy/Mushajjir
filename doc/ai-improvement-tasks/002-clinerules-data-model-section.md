# Problem
`.clinerules` only documents workflow conventions (ESLint, Prettier, git). It does not document the tree data model, edge kinds, or store computed pipeline that agents need to work effectively.

# Improvement Needed
Add a "Data Model" section to `.clinerules` documenting node shape, edge kinds, settings schema, and computed property dependencies.

# Expected Result
Future AI agents start with correct data model knowledge instead of discovering it through code exploration.
